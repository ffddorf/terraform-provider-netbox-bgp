overlay: 1.0.0
info:
  title: Overlay for TF generator
  version: 0.0.0
actions:
  - target: "$.components.schemas.*.properties.*[?(@.oneOf)]"
    update:
      type: integer
      description: Reference to foreign ID
      oneOf: null
  - target: "$.components.schemas.*.properties.tags.items"
    remove: true
  - target: "$.components.schemas.*.properties.tags"
    update:
      items:
        type: string
  # changes to RoutingPolicyRule
  - target: "$.components.schemas.RoutingPolicyRule.properties.match_custom"
    update:
      type: string
  - target: "$.components.schemas.RoutingPolicyRule.properties.set_actions"
    update:
      type: string
  - target: "$.components.schemas.RoutingPolicyRuleRequest.properties.match_custom"
    update:
      type: string
  - target: "$.components.schemas.RoutingPolicyRuleRequest.properties.set_actions"
    update:
      type: string
  # fixes to the schema (potentially upstream)
  - target: "$.components.schemas.WritableBGPSessionRequest.required"
    update:
      - "status"
