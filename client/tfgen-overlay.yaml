overlay: 1.0.0
info:
  title: Overlay for TF generator
  version: 0.0.0
actions:
  - target: "$.components.schemas.*.properties.*[?(@.oneOf)]"
    update:
      type: integer
      description: Reference to foreign ID
      oneOf: null
  - target: "$.components.schemas.*.properties.tags.items"
    remove: true
  - target: "$.components.schemas.*.properties.tags"
    update:
      items:
        type: string
