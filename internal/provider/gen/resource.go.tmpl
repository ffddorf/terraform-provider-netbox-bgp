// Code generated by internal/provider/gen DO NOT EDIT.

package provider

import (
	"context"
	"net/http"

	"github.com/ffddorf/terraform-provider-netbox-bgp/internal/resource_{{ .Name }}"
	"github.com/hashicorp/terraform-plugin-framework/path"
	"github.com/hashicorp/terraform-plugin-framework/resource"
)

{{- $typeName := printf "%sResource" (.Name | toPublicName) }}

// Ensure provider defined types fully satisfy framework interfaces.
var _ resource.Resource = &{{ $typeName }}{}
var _ resource.ResourceWithImportState = &{{ $typeName }}{}

func New{{ $typeName }}() resource.Resource {
	return &{{ $typeName }}{}
}

// SessionResource defines the resource implementation.
type {{ $typeName }} struct {
	client *ProviderClient
}

// Metadata implements resource.Resource.
func (*{{ $typeName }}) Metadata(ctx context.Context, req resource.MetadataRequest, resp *resource.MetadataResponse) {
	resp.TypeName = req.ProviderTypeName + "_{{ .Name }}"
}

// Schema implements resource.Resource.
func (*{{ $typeName }}) Schema(ctx context.Context, req resource.SchemaRequest, resp *resource.SchemaResponse) {
	resp.Schema = resource_{{ .Name }}.{{ .Name | toPublicName }}ResourceSchema(ctx)
}

func (r *{{ $typeName }}) Configure(ctx context.Context, req resource.ConfigureRequest, resp *resource.ConfigureResponse) {
	r.client = configureResourceClient(req, resp)
}

// Read implements resource.Resource.
func (r *{{ $typeName }}) Read(ctx context.Context, req resource.ReadRequest, resp *resource.ReadResponse) {
	var data resource_{{ .Name }}.{{ .Name | toPublicName }}Model
	resp.Diagnostics.Append(req.State.Get(ctx, &data)...)
	if resp.Diagnostics.HasError() {
		return
	}

	parsed, err := r.client.PluginsBgp{{ .APIName }}RetrieveWithResponse(ctx, int(data.Id.ValueInt64()))
	res := MaybeAPIError("failed to fetch {{ .Name }}", err, parsed.JSON200, parsed.HTTPResponse, parsed.Body, resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	data.FillFromAPIModel(ctx, res, resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &data)...)
}

// Create implements resource.Resource.
func (r *{{ $typeName }}) Create(ctx context.Context, req resource.CreateRequest, resp *resource.CreateResponse) {
	var data resource_{{ .Name }}.{{ .Name | toPublicName }}Model
	resp.Diagnostics.Append(req.Plan.Get(ctx, &data)...)
	if resp.Diagnostics.HasError() {
		return
	}

	params := data.ToAPIModel(ctx, resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	parsed, err := r.client.PluginsBgp{{ .APIName }}CreateWithResponse(ctx, params)
	res := MaybeAPIError("failed to create {{ .Name }}", err, parsed.JSON201, parsed.HTTPResponse, parsed.Body, resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	data.FillFromAPIModel(ctx, res, resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &data)...)
}

// Update implements resource.Resource.
func (r *{{ $typeName }}) Update(ctx context.Context, req resource.UpdateRequest, resp *resource.UpdateResponse) {
	var data resource_{{ .Name }}.{{ .Name | toPublicName }}Model
	resp.Diagnostics.Append(req.Plan.Get(ctx, &data)...)
	var id int64
	resp.Diagnostics.Append(req.State.GetAttribute(ctx, path.Root("id"), &id)...)
	if resp.Diagnostics.HasError() {
		return
	}

	params := data.ToAPIModel(ctx, resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}
	parsed, err := r.client.PluginsBgp{{ .APIName }}UpdateWithResponse(ctx, int(id), params)
	res := MaybeAPIError("failed to update {{ .Name }}", err, parsed.JSON200, parsed.HTTPResponse, parsed.Body, resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	data.FillFromAPIModel(ctx, res, resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &data)...)
}

// Delete implements resource.Resource.
func (r *{{ $typeName }}) Delete(ctx context.Context, req resource.DeleteRequest, resp *resource.DeleteResponse) {
	var data resource_{{ .Name }}.{{ .Name | toPublicName }}Model
	resp.Diagnostics.Append(req.State.Get(ctx, &data)...)
	if resp.Diagnostics.HasError() {
		return
	}

	parsed, err := r.client.PluginsBgp{{ .APIName }}DestroyWithResponse(ctx, int(data.Id.ValueInt64()))
	toCheck := parsed
	if parsed.StatusCode() != http.StatusNoContent {
		toCheck = nil // response not usable
	}
	MaybeAPIError("failed to delete {{ .Name }}", err, toCheck, parsed.HTTPResponse, parsed.Body, resp.Diagnostics)
}

// ImportState implements resource.ResourceWithImportState.
func (p *{{ $typeName }}) ImportState(ctx context.Context, req resource.ImportStateRequest, resp *resource.ImportStateResponse) {
	importByInt64ID(ctx, req, resp)
}
